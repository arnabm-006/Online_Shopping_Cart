# Online_Shopping_Cart
This Python script runs a console-based Online Shopping Cart Application. It utilizes an immutable tuple of tuples named PRODUCTS to store a fixed item catalog and a dynamic list of tuples named cart to keep track of a user's chosen purchases.
